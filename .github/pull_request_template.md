## Summary

What does this PR change and why?

## Checklist

- [ ] `npm test` passes (added/updated tests where relevant)
- [ ] `npm run lint` passes
- [ ] Input from clients is validated; errors return proper HTTP status codes
- [ ] No secrets (API keys, gateway keys, `DATA_ENCRYPTION_KEY`) or runtime
      stores (`team.json`, `conversations.json`, `logs/`) committed
- [ ] Updated docs if behavior changed (`README.md`, `docs/`, `TAI_LIEU.md`)
- [ ] Updated `CHANGELOG.md` under `[Unreleased]`
